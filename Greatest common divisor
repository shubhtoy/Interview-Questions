#include <bits/stdc++.h>
using namespace std;
int gcd(int a, int b){
       if(a%b==0) return b;
       return gcd(b,a%b); 
    }

 
int main(){
    cout<<"enter n1 and n2"<<endl;
    int a,b;
    cin>>a>>b;
    cout<<gcd(a,b)<<endl;

}
// relation between lcm and gcd
// lcm(a,b) *gcd(a,b) =a*b
